import 'package:flutter/material.dart';

class DonateDetail2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('마이 페이지'),
        ),
      ),
    );
  }
}